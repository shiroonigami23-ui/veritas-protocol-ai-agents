# Contributing

## Workflow
1. Fork and create a feature branch.
2. Keep changes mapped to requirement IDs (FR-*, NFR-*).
3. Add or update tests for every behavior change.
4. Run `python -m unittest discover -s tests -v` before opening a PR.
